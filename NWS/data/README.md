# Local FEISTY inputs

Place the following two existing research files in this directory only when rerunning the full FEISTY simulation:

```text
nws_for_feisty_proj_year.RDS
all_FFMSY.RDS
```

They are deliberately excluded from version control because of their size. They are not required to render the Quarto report from the final outputs included in this repository.
